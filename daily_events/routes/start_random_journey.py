async def start_random_journey():
    # NOTE: check that the user has the right JWT and that, if they
    # have the start_one permission, it hasn't already been consumed by a different
    # jwt. do not check entitlements.
    ...
